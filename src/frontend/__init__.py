"""Frontend module — the Streamlit trace view (`streamlit run src/frontend/app.py`).

`view_models.py` and `diagnosis_service.py` are pure/Streamlit-independent and
unit-tested; `app.py`/`graph_render.py`/`detail_panel.py` are the Streamlit
UI layer, verified by running the app directly.
"""
